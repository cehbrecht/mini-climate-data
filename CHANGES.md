# Changes

All notable changes to `mini-climate-data` are recorded here.

This project is in early scaffold form, so entries may describe development
milestones before formal releases begin.

## Unreleased

- Added the initial package, recipe, reducer, validation, registry, and CLI scaffold.
- Added the `write_text`, `xarray_subset`, and `ncks_subset` reducers.
- Added recipe schema validation and an example smoke-test recipe.
- Added optional dependency groups for fetching, NetCDF reducers, intake sources, and full runtime integrations.
- Added a conda environment for local development.
- Added CI checks for linting and tests.

